---
layout: _layout.njk
title: 일등도시가스 | 도시가스시공 > 시공후기
permalink: /gallery.html
---

<div class="page-title-wrap">
	<div class="container">
		<div>
			<h2 class="page-title">시공후기</h2>
			안전하고 깔끔한 시공사례를 확인해보세요.
		</div>
	</div>
</div>

<section class="container section-sub">
	<ul class="gallery-list">
		{% for i in range(0, 13) %}
		<li>
			<a href="javascript:SetStorage({{ i }}, 2)" title="제목">
				<div class="img-wrap">
					<img src="asset/images/gallery/g_{{ i }}_1.png" alt="제목" />
				</div>
				<div class="cont-wrap">
					<h4 id="title_{{ i }}" class="title">시공업체명 {{ i }}</h4>
					<h5 id="subTitle_{{ i }}" class="title-sub">시공업체{{ i }} 블로그 게시글의 첫 줄을 작성하면 됩니다. 길게 작성하더라도 한줄만 보이고, 2줄 이상 보이게 하고싶으면 연락주세요.</h5>
					<div id="hashTag_{{ i }}" class="hidden">강동구도시가스 도시가스시공 시공업체</div>
				</div>
			</a>
		</li>
		{% endfor %}
	</ul>
</section>
<script>
	function SetStorage(index, count) {
		var title = $("#title_" + index).html();
		var subTitle = $("#subTitle_" + index).html();
		var hashTag = $("#hashTag_" + index).html();

		localStorage.setItem("title", title);
		localStorage.setItem("subTitle", subTitle);
		localStorage.setItem("hashTag", hashTag);
		location.href = "/galleryDetails?idx=" + index + "&imgCount=" + count;
	}
</script>
